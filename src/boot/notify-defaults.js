import { Notify } from 'quasar'

Notify.setDefaults({
  actions: [
        {
            icon: 'eva-close-outline',
            color: 'white',
            round: true,
            dense: true,
            style: 'opacity: 0.7'
        }
    ]
})